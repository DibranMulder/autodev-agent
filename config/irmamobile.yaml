# Configuration for irmamobile repository
repository:
  name: irmamobile
  owner: privacybydesign
  language: Dart
  test_command: flutter test
  lint_command: flutter analyze

  focus_areas:
    # Age Verification UI (HIGHEST PRIORITY)
    - Age verification consent screens
    - age_over_X attribute display
    - Age verification credential cards
    - Zero-knowledge age proof UI
    - Age verification flow UX

    # Protocol support in mobile
    - OpenID4VCI credential issuance UI
    - OpenID4VP presentation flows
    - SIOPv2 wallet authentication
    - QR code protocol handling

    # Credential format display
    - SD-JWT credential rendering
    - mdoc/mDL credential display
    - Selective disclosure UI

  excluded_paths:
    - build/
    - .dart_tool/
    - .git/
    - ios/Pods/
    - android/.gradle/

# Specific improvement rules for irmamobile
rules:
  age_verification:
    - Add age verification credential card design
    - Implement age_over_X attribute display
    - Add clear consent UI for age disclosure
    - Show only "over 18" vs full birthdate (privacy)
    - Add age verification success/failure feedback

  protocols:
    - Add OpenID4VCI issuance flow UI (CHECK IF IN PROGRESS)
    - Implement OpenID4VP presentation consent
    - Support credential offer deep links
    - Handle authorization request URIs

  credential_formats:
    - Display SD-JWT credentials with disclosure options
    - Render mdoc/mDL credentials
    - Implement selective attribute disclosure UI

  testing:
    - Age verification flow widget tests
    - Widget tests for protocol flow screens
    - Integration tests for issuance/presentation
